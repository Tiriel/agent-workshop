{% extends 'admin_base.html.twig' %}

{% block title %}{{ shortName|capitalize }} Management{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col-12 d-flex justify-content-between align-items-center">
        <h1 class="display-5 fw-bold mb-0">{{ shortName|capitalize }} Management</h1>
        <a href="{{ path("app_admin_#{shortName}_new") }}" class="btn admin-btn-primary text-white">
            <i class="bi bi-plus-circle"></i> Create New {{ shortName|capitalize }}
        </a>
    </div>
</div>

{{ component('Admin:'~componentName, {paginated: true, currentPage: app.request.query.get('page', 1)}) }}
{% endblock %}
