{% extends 'base.html.twig' %}

{% block title %}Tag Management{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col-12 d-flex justify-content-between align-items-center">
        <h1 class="display-5 fw-bold mb-0">Tag Management</h1>
        <a href="{{ path('app_admin_tag_new') }}" class="btn admin-btn-primary text-white">
            <i class="bi bi-plus-circle"></i> Create New Tag
        </a>
    </div>
</div>

<twig:Admin:TagList :paginated="true" />
{% endblock %}
