{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col-12">
        <a href="{{ path('app_admin_user_index') }}" class="text-decoration-none text-muted mb-3 d-inline-block">
            ← Back to users
        </a>
    </div>
</div>

<div class="row">
    <div class="col-12 col-lg-8">
        <div class="card admin-card shadow-sm border-0">
            <div class="card-header bg-white">
                <h2 class="h4 mb-0">Edit User</h2>
            </div>
            <div class="card-body">
                {{ include('admin/user/_form.html.twig', {'button_label': 'Update User'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
